# 电脑游戏转手机网页版 · 通用方法手册

> 原理：很多"电脑游戏"其实是**网页技术**做的（网页游戏套了个壳）。只要能判断出来，就能部署成网页，用手机浏览器直接玩。
> 本手册基于《乡村狂想曲》（Cocos2d-js + Electron）实战整理，2026-08。
> 适用范围：Cocos Creator / Unity WebGL / 纯 HTML5 游戏等一切"网页内核"游戏。

---

## 〇、核心思路（记住这四步）

1. **判断**：游戏是不是网页技术做的？
2. **跑起来**：把网页目录用静态服务器托管，浏览器能打开。
3. **手机没鼠标**：把"触摸"翻译成游戏认识的"鼠标事件"。
4. **手机没键盘**：画虚拟按键，把"按键"灌进引擎的键盘通道。

> 只要游戏目录里有 index.html / 网页 JS / 网页资源，基本就能走通这条路。

---

## 一、怎么判断游戏是不是"网页游戏套壳"

打开游戏安装目录，看有没有这些特征（有 2 个以上基本就是）：

| 特征 | 说明 |
|---|---|
| index.html | 网页入口，铁证 |
| cocos2d-js*.js + assets/ | Cocos Creator 2.x web 版 |
| Build/ + *.unityweb + index.html | Unity WebGL |
| resources/app/、*.pak、icudtl.dat、chrome_100_percent.pak | Electron 套壳（内核就是浏览器） |
| 目录里有大量 .png/.js/.json 网页资源 | 网页游戏特征 |

**如果不是网页内核**（纯 C++/Unreal/原生 exe，目录里没有网页文件）→ 此方法不适用，只能走"串流"（Steam Link / Moonlight / 向日葵 手机远程玩电脑）。

---

## 二、跑起来：静态服务器

```bash
# 在游戏网页目录（含 index.html 的那层）执行：
python3 -m http.server 8010 --bind 0.0.0.0
```

- 0.0.0.0 允许手机访问（不加只能本机）；
- 浏览器打开 http://localhost:8010/index.html 验证**电脑能玩**（这是所有后续步骤的前提）；
- 写个 .bat 双击启动更方便：
  ```bat
  @echo off
  cd /d "%~dp0"
  python3 -m http.server 8010 --bind 0.0.0.0
  ```

**手机访问**：手机和电脑连同一 WiFi → 手机浏览器打开 http://<电脑IP>:8010/index.html 。
查 IP：Win+R → cmd → ipconfig → 找 IPv4 地址。
打不开就放行防火墙（管理员 CMD）：
```
netsh advfirewall firewall add rule name="gameweb" dir=in action=allow protocol=TCP localport=8010
```

---

## 三、存档问题（先搞清楚再改）

网页游戏的存档常见位置：

| 位置 | 特点 |
|---|---|
| 浏览器 localStorage / IndexedDB | 网页版天然能用，可能优先读取 |
| 游戏目录里的存档文件 | 需要看代码逻辑是否读写 |
| Electron 内部存储（C:/Users/<你>/AppData/Roaming/<应用名>/Local Storage/leveldb） | Chromium 数据库，可用 Node 的 classic-level 读写 |

**实战经验**：
- 很多游戏**双写**（localStorage + 磁盘文件），读取时 **localStorage 优先** —— 改磁盘文件没用，得改 localStorage；
- 网页版登录没有 Steam ID 等，存档键可能和原生版不同 → 把网页版代码里的存档键改成和原生一致，或迁移存档；
- 导出 Electron 存档注入网页版：用 classic-level 读数据库 → 导出 JSON → 网页首次打开时自动写入 localStorage（写个自动导入脚本）。

---

## 四、手机没鼠标 → 模拟点击（最常卡住的一步）

**症状**：电脑浏览器一切正常，手机点按钮没反应；尤其"鼠标悬停才生效"的按钮（悬停高亮的那种）。

**原因**：游戏监听"鼠标事件"（mousedown/mousemove/mouseup），手机触摸产生的是"触摸事件"（touchstart/touchend），游戏收不到。

**推荐解法（改游戏按钮的基础组件，最干净）**：
找到游戏按钮的事件基础组件（Cocos 常见是 MouseEvent 组件），给它加触摸监听，把触摸翻译成鼠标：

```js
// 组件启用时注册触摸
onEnable(){
  this.node.on(cc.Node.EventType.TOUCH_START, this.onTouchStart, this);
  this.node.on(cc.Node.EventType.TOUCH_END,   this.onTouchEnd,   this);
},
// 手指按下 → 判断是否按在按钮内 → 相当于"鼠标悬停+按下"
onTouchStart(e){
  this._touched = this.node.getBoundingBoxToWorld().contains(e.getLocation());
  this._touched && this.fire(MOUSE_DOWN, e);
},
// 手指抬起 → 触发"鼠标抬起" = 完成一次点击
onTouchEnd(e){
  this._touched && this.fire(MOUSE_UP, e);
  this._touched = false;
}
```

**必踩的坑**：
1. **触摸事件对象没有 getButton()**（鼠标事件才有），按钮回调里调用会报错中断 → 补丁：
   ```js
   cc.Event.EventTouch.prototype.getButton = function(){ return 0; };  // 0=左键
   ```
2. **别给"全局鼠标层"加触摸转发**（除非加 try/catch 隔离）——容易破坏引擎原生触摸按钮（"本来能点的按钮反而点不了"）。尽量只改**具体按钮组件**。
3. **每改一步必须在手机上验证**，不要攒着一起改。

---

## 五、手机没键盘 → 虚拟按键

**症状**：游戏需要键盘（WASD/方向键移动、Shift 跑、空格交互等）。

**原理**：游戏监听**引擎的键盘事件**（Cocos 2.x 是 cc.systemEvent）。电脑键盘从这里进游戏，我们也从这里进。

**解法**：页面层画 DOM 按钮（固定定位、高 z-index、半透明），触摸按下/抬起时向引擎发键盘事件：

```js
// 发键盘事件（Cocos 2.x 写法）
function keyEv(type, keyCode){
  var evType = (type === 'keydown')
    ? cc.SystemEvent.EventType.KEY_DOWN
    : cc.SystemEvent.EventType.KEY_UP;
  cc.systemEvent.emit(evType, { keyCode: keyCode, preventDefault: function(){} });
}
// 绑定：按住=按下，松开=抬起
function bindKey(el, keyCode){
  var on = false;
  el.addEventListener('touchstart', function(){ if(!on){ on=true; keyEv('keydown', keyCode); } }, {passive:false});
  el.addEventListener('touchend',   function(){ if(on){ on=false; keyEv('keyup',   keyCode); } });
}
// 按键码：左37 上38 右39 下40   Shift=16   WASD=87/65/83/68
bindKey(up, 38); bindKey(down, 40); bindKey(left, 37); bindKey(right, 39);
```

**关键坑**：
- **千万别往浏览器 DOM 发合成 KeyboardEvent**——手机上游戏引擎根本不监听 DOM 键盘事件（它认为手机没物理键盘），信号发出去没人收。必须走**引擎自己的事件总线**（如 cc.systemEvent.emit）；
- 按住持续移动的原理：引擎收到一次"按下"会记录按键状态，每帧读取 → 所以只需要"按下发一次、松开发一次"，不用重复发；
- 不同引擎的入口：Cocos 2.x = cc.systemEvent.emit；Cocos 3.x = input.emit / input.on；Unity WebGL 一般内置触摸支持，键盘才需要虚拟。

---

## 六、不同引擎速查

| 引擎 | 特征文件 | 键盘入口 | 鼠标/触摸 |
|---|---|---|---|
| Cocos Creator 2.x | cocos2d-js-min*.js、cc._RF | cc.systemEvent.emit(KEY_DOWN/UP, {keyCode}) | 组件监听 cc.Node.EventType.TOUCH_*，触摸对象补 getButton |
| Cocos Creator 3.x | cocos-js/*.js、cc.env | input.emit(Input.EventType.KEY_DOWN, ...) | input.on(Input.EventType.TOUCH_*, ...) |
| Unity WebGL | Build/*.unityweb、index.html | 引擎自动处理大部分，可能需要虚拟键 | 通常内置触摸，一般不用改 |
| 纯 HTML5/JS 游戏 | index.html + 手写 JS | 看代码监听什么（keydown 或引擎封装） | 看代码监听什么 |

> 万变不离其宗：**找到游戏真正监听的"事件通道"，从那个通道喂事件**。

---

## 七、标准操作流程（清单）

1. **备份**：整个网页目录复制一份（改坏了随时还原）—— 永远第一步；
2. **起服务器**，电脑浏览器验证能玩（存档/画面/交互）；
3. **手机访问**：同 WiFi + IP + 防火墙放行；
4. **按顺序解决**：能进主界面 → 能点按钮 → 有存档 → 能移动 → 能交互；
5. **每改一处就手机验证一次**，记录改了什么（写进文档，方便还原）；
6. 全部 OK 后，写一份"操作说明.md"（启动方法 + 手机操作 + 改动清单）。

---

## 八、常见问题速查

| 现象 | 原因 | 解法 |
|---|---|---|
| 电脑能玩，手机点按钮没反应 | 游戏只听鼠标事件，触摸没翻译 | 按钮组件加触摸监听（见四） |
| 改完手机"连原来能点的都点不了" | 触摸转发破坏了引擎触摸 | 只改具体组件；全局层加 try/catch 隔离；回退重来 |
| 手机点按钮报错/没反应但光标动了 | 触摸事件缺 getButton() | 补 EventTouch.prototype.getButton |
| 电脑键盘能移动，手机虚拟键没反应 | 键盘事件发到了 DOM 而非引擎 | 用引擎事件总线（cc.systemEvent.emit） |
| 改了存档文件但游戏不读 | 游戏读的是 localStorage | 改 localStorage / 注入存档 |
| 服务器关了网页还能开 | 浏览器缓存 | 无痕窗口验证；手机访问验证 |
| 手机打不开 | IP 错/不同网/防火墙 | 查 ipconfig、同 WiFi、放行端口 |

---

## 九、我的工具箱（本次用到的）

- **静态服务器**：python3 -m http.server
- **改游戏代码**：Python 字节级替换（游戏代码是压缩/混淆的，用精确字符串替换）
- **读 Electron localStorage**：Node.js classic-level（读 Chromium leveldb 数据库）
- **手机验证缓存问题**：浏览器无痕窗口
- **防火墙放行**：netsh advfirewall firewall add rule name="xxx" dir=in action=allow protocol=TCP localport=端口

---

## 十、红线提醒

- 仅供**自己本地游玩/研究**使用；不要把改造后的游戏公开分发（有版权问题）；
- 修改的是游戏文件，**务必先备份**；
- 游戏如果升级/重装，改动会丢，重新打补丁即可（有文档就不怕）。
